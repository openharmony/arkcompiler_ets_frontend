/*---
desc: Private interface methods
---*/

interface Vehicle {
  private static getHorsePower(rpm: int, torque: int): int {
    return (rpm * torque) / 5252;
  }
  private getHorsePower2(rpm: int, torque: int): int {
    return (rpm * torque) / 5252;
  }
}

function main(): void {}